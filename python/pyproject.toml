[project]
name = "qoder-agent-sdk-samples-python"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "qoder-agent-sdk>=1.0.10,<2",
]

[dependency-groups]
dev = [
    "asyncpg>=0.29,<1",
    "mypy>=1.17,<2",
    "redis>=5,<9",
    "ruff>=0.12,<1",
]

[tool.uv]
package = false

[tool.ruff]
target-version = "py310"
line-length = 88

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "PTH"]

[tool.mypy]
python_version = "3.10"
strict = true

[[tool.mypy.overrides]]
module = ["asyncpg"]
ignore_missing_imports = true
